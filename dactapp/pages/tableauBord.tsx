import styles from "@/styles/TableauBord.module.css";

// Page tableau de bord
const TableauBord = () => {
  return (
    <div className={styles.container}>
      <h2>Tableau de bord</h2>
    </div>
  );
};

export default TableauBord;
